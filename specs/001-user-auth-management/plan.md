# Implementation Plan: User Authentication & Management

**Branch**: `001-user-auth-management` | **Date**: 2026-05-14 | **Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `/specs/001-user-auth-management/spec.md`

## Summary

Implement a complete email/password authentication and user management system for InnovatEPAM Portal. Users self-register with `@epam.com` addresses and receive the `submitter` role; a seed script provisions `admin` accounts. Sessions are cookie-based with an 8-hour sliding window (iron-session); expired sessions redirect to login with the original URL preserved and a session-expired message shown. Role-based middleware protects all routes and redirects violations to `/access-denied` (title: `Access denied`; message: `You do not have permission to access this page.`). Account lockout after 5 failed logins shows a human-readable remaining-time message formatted with date-fns. Admins can deactivate user accounts via a Server Action with visible confirmation. Stack: Next.js 15 App Router · Drizzle ORM + better-sqlite3 · iron-session · bcryptjs · shadcn/ui · Tailwind CSS v4 `@theme` · date-fns.

## Technical Context

**Language/Version**: TypeScript 5.x / Next.js 15 (App Router) / Node.js 20 LTS

**Primary Dependencies**: `next@15`, `react@19`, `tailwindcss@4`, `@tailwindcss/postcss`, `shadcn/ui`, `drizzle-orm`, `better-sqlite3`, `iron-session`, `bcryptjs`, `date-fns@3`, `zod`, `react-hook-form`, `@hookform/resolvers`

**Storage**: SQLite — single file at `data/innovatepam.db` — managed by Drizzle ORM with `better-sqlite3` driver; migrations via `drizzle-kit`

**Testing**: Vitest + React Testing Library + `@testing-library/user-event` (unit/component); Playwright (E2E); integration tests against a real SQLite test DB file

**Target Platform**: Web browser, desktop + mobile-first (Next.js SSR + RSC + Server Actions)

**Project Type**: Full-stack Next.js web application

**Performance Goals**: Login Server Action round-trip < 500 ms p95; Registration action < 1 s p95

**Constraints**: TypeScript `"strict": true`; `@epam.com` domain enforced server-side; no external auth services; single SQLite file; bootcamp timeline

**Scale/Scope**: Bootcamp prototype; 2 roles (submitter / admin); < 100 concurrent users

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

- [x] **I. Clean Code** — Server Actions are single-purpose; each `lib/` module has one responsibility; zod schemas co-located with validation logic; no dead code.
- [x] **II. Simple UI/UX** — Tailwind v4 `@theme` utility classes only; shadcn/ui components for all form primitives; mobile-first layouts across all breakpoints.
- [x] **III. Minimal Dependencies** — Every dependency justified (see research.md R01–R05); no dependency duplicates a native Next.js capability.
- [x] **IV. Accessibility** — shadcn/ui form components include ARIA labels; error messages associated with inputs via `aria-describedby`; focus managed after submission; keyboard-navigable nav.
- [x] **V. Error Handling** — All Server Actions return typed `ActionResult` unions (never throw); `error.tsx` and `not-found.tsx` present at all route segments that fetch data; loading states via React Suspense. All auth screens (registration, login, access-denied, deactivated-account) MUST render loading and error states; no blank or empty auth screen is permitted.
- [x] **VI. ADRs** — Four ADRs created before tasks.md: `adr-0001` (SQLite/Drizzle), `adr-0002` (iron-session), `adr-0003` (Tailwind v4 @theme), `adr-0004` (bcryptjs).
- [x] **VII. TypeScript Strict Mode** — `"strict": true` in `tsconfig.json`; all entities fully typed via Drizzle inference; `===` throughout; nullish coalescing for optional fields; no `any`.
- [x] **Testing** — Vitest + RTL for unit/component tests; Playwright for E2E (registration, login, role-enforcement flows); integration tests against real SQLite test DB; CI gates: `type-check`, `lint`, `test`, `playwright test`.
- [x] **Stack** — Next.js 15 App Router + React 19 + Tailwind CSS v4 + shadcn/ui + TypeScript strict mode.

## Project Structure

### Documentation (this feature)

```text
specs/001-user-auth-management/
├── plan.md              ✅ This file
├── research.md          ✅ Phase 0 output
├── data-model.md        ✅ Phase 1 output
├── quickstart.md        ✅ Phase 1 output
├── contracts/
│   └── server-actions.md ✅ Phase 1 output
└── tasks.md             (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
app/
├── (auth)/
│   ├── login/
│   │   └── page.tsx              # Login page (RSC wrapper)
│   ├── register/
│   │   └── page.tsx              # Registration page (RSC wrapper)
│   └── layout.tsx                # Unauthenticated shell — centered card layout
├── (protected)/
│   ├── dashboard/
│   │   └── page.tsx              # Submitter dashboard
│   ├── admin/
│   │   ├── dashboard/
│   │   │   └── page.tsx          # Admin dashboard
│   │   └── users/
│   │       └── page.tsx          # User list + deactivation UI
│   ├── access-denied/
│   │   └── page.tsx              # Role-violation landing page
│   └── layout.tsx                # Authenticated shell — nav + session guard
├── error.tsx                     # Root error boundary
├── not-found.tsx                 # 404 boundary
└── layout.tsx                    # Root layout (fonts, Tailwind @theme, globals)

components/
├── auth/
│   ├── LoginForm.tsx             # Client component — react-hook-form + zod
│   ├── RegisterForm.tsx          # Client component — react-hook-form + zod
│   └── LogoutButton.tsx          # Client component — calls logoutAction
└── ui/                           # shadcn/ui components (copy-into-repo)

lib/
├── auth/
│   ├── session.ts                # iron-session config; getSession(); requireAuth(); requireRole()
│   ├── password.ts               # bcryptjs hash() and verify() wrappers
│   └── validation.ts             # zod: registerSchema, loginSchema
└── db/
    ├── index.ts                  # Drizzle client singleton
    ├── schema.ts                 # users table (Drizzle schema + inferred types)
    ├── migrations/               # drizzle-kit generated SQL files
    └── seed.ts                   # Admin account seeder (npm run db:seed)

actions/
└── auth.ts                       # Server Actions: registerAction, loginAction,
                                  # logoutAction, deactivateUserAction

middleware.ts                     # RBAC: route protection, role-based redirects,
                                  # returnUrl preservation

data/
└── .gitkeep                      # DB file written here at runtime (gitignored)

tests/
├── integration/
│   └── auth/
│       ├── register.test.ts      # registerAction against real SQLite test DB
│       ├── login.test.ts         # loginAction: credentials, lockout, deactivated
│       └── deactivate.test.ts    # deactivateUserAction: admin gating, status change
└── e2e/
    ├── registration-flow.spec.ts       # US1 full browser flow
    ├── login-flow.spec.ts              # US2 full browser flow
    └── admin-role-enforcement.spec.ts  # US3 RBAC browser flow

docs/
└── adrs/
    ├── adr-0001-sqlite-drizzle-decision.md         # SQLite + Drizzle ORM
    ├── adr-0002-iron-session-auth-decision.md      # iron-session for auth sessions
    ├── adr-0003-tailwind-theme-decision.md         # Tailwind CSS v4 with @theme
    └── adr-0004-bcrypt-password-hashing-decision.md # bcryptjs for password hashing
```

**Structure Decision**: Single Next.js App Router project. Route groups `(auth)` and `(protected)` provide separate layout shells without URL segments. `lib/` strictly separates auth concerns from database concerns. Server Actions are the only write path — no REST API needed for this feature. SQLite single-file is appropriate for bootcamp scope.

## Complexity Tracking

No constitution violations. All dependency additions are justified in `research.md` and recorded in ADRs.
